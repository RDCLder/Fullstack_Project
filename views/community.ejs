<!DOCTYPE html>
<html lang="en">

<head>
    <% include partials/head.ejs %>
</head>

<body>
    <header>
        <% include partials/navbar.ejs %>
        <% include partials/sidebar.ejs %>
    </header>
    <div class="container-fluid m-0 p-0">
        <div class="row m-0 p-0">

            <div class="col-1"></div>

            <!-- Community Main Content-->
            <div class="col-6 m-0 p-0 communityMain">
                <% if (noTopics == false) { %>
                <% for (var i = 0; i < topics.length; i ++) { %>
                <% var topic = topics[i].dataValues %>
                <% var topicTime = topicTimes[i] %>
                <% include partials/cards/topicText.ejs %>
                <% } %>
                <% } %>
            </div>

            <!-- Community Side Content-->
            <div class="col-4 m-0 p-0 communitySide">
                <% include partials/cards/comDesc.ejs %>
                <% include partials/cards/comRule.ejs %>
                <% include partials/cards/comMod.ejs %>
            </div>

            <div class="col-1"></div>

        </div>
    </div>
    <% include partials/cdn.ejs %>
    <% include partials/functions.ejs %>
</body>

</html>